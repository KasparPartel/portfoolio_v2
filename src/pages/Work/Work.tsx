import workStyles from "./Work.module.css";

const Work = () => {
  return (
    <section id="work" className={workStyles.section}>
      <h2>My Work</h2>
    </section>
  );
};

export default Work;
